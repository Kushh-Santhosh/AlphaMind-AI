apiVersion: v1
kind: ConfigMap
metadata:
  name: alphamind-staging-config
  namespace: alphamind-staging
data:
  ENVIRONMENT: "staging"
  LOG_LEVEL: "INFO"
  ENABLE_PROMETHEUS_METRICS: "true"
  SSE_TICK_INTERVAL_SECONDS: "3.0"
